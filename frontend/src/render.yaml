services:
  - type: web
    name: my-frontend
    buildCommand: npm run build
    startCommand: npm start
    routes:
      - src: /((?!api).*)   # Rewrite all non-api requests
        dest: /index.html